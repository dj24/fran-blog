{
  "host": "${process.env.APP_HOST || 'localhost'}",
  "port": "${process.env.NODE_PORT || 1337}",
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
